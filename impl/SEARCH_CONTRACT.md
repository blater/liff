# Liff lookup contract

The lookup contract has moved into the authoritative
[product and implementation specification](SPECIFICATION.md). This compatibility
file remains so existing links continue to resolve.

Shared executable conformance cases remain in [`search-cases.json`](search-cases.json).
